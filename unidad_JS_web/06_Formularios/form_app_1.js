import { Formulario } from './formulario.js' // importamos la clase Formulario desde el fichero js
window.addEventListener('load', () => new Formulario()) // instanciamos un objeto Formulario cuando se cargue la página