// los nombres en js utilizan camelCase ("escritura camello")
// los paréntesis indican funcion
// los corchetes es el contenido de la función
// una función no se ejecuta hasta que no se llame a la función

function saludar() {
    console.log('hola desde app.js')
}

/* para invocar o llamar a la función, los parétesis son el operador de la función, invocan la función: saludar(); */